// Interactive component gallery — run with `npm run demo`.
// Dev-only: not part of the published package (tsup bundles src/index.ts).
import React from "react"
import { render } from "ink"
import { Gallery } from "./Gallery.js"

render(<Gallery />)
